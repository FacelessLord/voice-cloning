### Датасет
- Использовать whisper (версию, которая выдаёт не обязательно правильный, но созвучный текст), чтобы получить транскрипцию текста - получим (filename, chunks)\[] 
- По полученным чанкам порезать исходные аудиофайлы и убрать тишину (если она остаётся после виспера) - получим (filename + \_i, wav-object. text)\[]
- На каждый элемент с помощью `librosa` создаём небольшие модификации - получаем (filename + \_i + \_augmentation, wav-object, text)\[]
	- Растягиваем/сжимаем файл в 0.9-1.1 раза - не должно сильно влиять на тональность
	- Добавить какой-то шум
- Сохраняем каждый элемент последовательности в файл с указанным в элементе названием + добавляем `filename: text` в индекс
- Датасет готов
